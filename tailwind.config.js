/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.html", // Ensures all HTML files in src and its subdirectories are scanned.
    "./src/**/*.js",
    './src/**/*.php',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

